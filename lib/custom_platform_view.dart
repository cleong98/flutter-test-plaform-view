import 'package:flutter/material.dart';

class CustomPlatformView extends StatelessWidget {
  const CustomPlatformView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Custom Platform View"),);
  }
}